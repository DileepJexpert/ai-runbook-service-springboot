# Contract versioning

Contract 2.1 is additive to 2.0: existing `runbook-data.json` and `runbook-evidence.json` filenames and 2.0 meanings remain valid. The files now represent the canonical Service Intelligence Model. Consumers must accept 2.0 and 2.1 and must not reinterpret 2.0 fields.
