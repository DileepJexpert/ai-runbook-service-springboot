# Artifact ownership

AI may create only `runbook-data.json`, `runbook-evidence.json`, and restricted `security-findings.json`. Deterministic code alone creates `runbook-delta.json`, `generation-report.json`, fingerprints, and rendered documents. Security findings are never mapped to the Production Support page.
