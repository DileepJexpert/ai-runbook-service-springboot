# AI Runbook Shared Contract

This repository is the source of truth shared by the Python CLI and Spring Boot service. It owns extraction requirements, JSON schemas, fixtures, normalization and stable-ID rules, deterministic presentation, manual-note markers, and support safety boundaries. Implementations must report contract version `2.0` and are tested against the same fixtures.
